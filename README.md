# assembleHDR
Merges RAW image files into HDR (high dynamic range) images. RAW files have minimal processing so the resulting HDR should be close to a sensor reading. 

Only tested with Canon .CRW and .CR2 images but should work with more.
